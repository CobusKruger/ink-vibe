<?php
/**
 * Pest configuration (NFR-9, Story 1.11).
 *
 * Auto-loaded by Pest from `tests/Pest.php`. The unit tests bind their own
 * Brain Monkey lifecycle per-file
 * (`beforeEach( fn () => Monkey\setUp() )` / `afterEach( fn () => Monkey\tearDown() )`),
 * so no global TestCase or shared setUp is imposed here. Pest discovers tests
 * through the `phpunit.xml` testsuites; this file is the conventional place a
 * later story (18.8) can register a base TestCase, shared expectations, or
 * datasets as the pyramid grows.
 *
 * @package Ink\Tests
 */

declare(strict_types=1);
