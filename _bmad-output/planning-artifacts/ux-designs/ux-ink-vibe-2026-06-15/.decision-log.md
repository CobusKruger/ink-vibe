# UX Decision Log — INK

Canonical record of UX decisions for this run. Spines (DESIGN.md / EXPERIENCE.md) are distilled from this log, `.working/`, `imports/`, and confirmed sources. Spines win on conflict.

---

## 2026-06-15 — Run kickoff

- **Intent:** Create (no prior UX run existed).
- **Working mode:** Fast path — subagent-extract confirmed sources, draft both spines with `[ASSUMPTION]` tags, review, optional reviewer gate, finalize. Creative tools skipped on the draft pass (design already largely locked via Lovable handoff).
- **Authoritative sources confirmed by Cobus:**
  - `docs/design-handoff/tokens/theme-tokens.json` + `token-map.md` — color/type/spacing tokens → DESIGN.md frontmatter.
  - `docs/design-handoff/page-map.csv` — surfaces / IA skeleton.
  - `docs/design-handoff/lovable-repo-analysis.md` + `agent-brief.md` — component inventory, design intent.
  - `docs/mockup-readiness-assessment.md` — mock-ready vs spine-only screens.
  - `docs/ui-copy-translations.md` — voice & tone, microcopy (Afrikaans-first).
  - `docs/afrikaans-terms.md` — terminology, mirrored verbatim.
  - `prd.md` + `docs/specs/ink-consolidated-spec.md` — features, journeys, states.
- **Standing constraints inherited from project-context.md:**
  - Afrikaans-first: front end entirely Afrikaans; never lift English placeholder copy from Lovable mockups; UI copy comes from `ui-copy-translations.md`.
  - Design tokens only — no hardcoded color/spacing/type; `theme.json` naming is production source of truth even where Lovable names differ.
  - Lovable = design *intent*, not code. Re-express in WP block primitives; never port JSX/Tailwind.
  - Heading casing: sentence case.

## 2026-06-15 — Spines distilled (fast path)

Both spines drafted directly from the confirmed sources via parallel extraction agents (visual identity + experience model). No creative tools run on this pass (design largely locked by Lovable handoff).

**DESIGN.md** — tokens carried verbatim from `theme-tokens.json`; canonical slugs from `token-map.md` where declared. Body: Brand & Style, Colors, Typography, Layout & Spacing, Elevation & Depth, Shapes, Components, Do's & Don'ts.

**EXPERIENCE.md** — full 16-surface IA from `page-map.csv` + roles; voice/tone from `ui-copy-translations.md`; behavioral component + state patterns (incl. THE conflation rule); interaction primitives; accessibility floor; responsive contract; UJ-1..6 flows; concerns scan.

### `[ASSUMPTION]` index — needs confirmation
1. Brand statement posture ("warm editorial") — inferred, no verbatim source sentence.
2. Typography role composition (size+weight+line-height per role) — sources give independent scales only; no `letterSpacing` token.
3. theme.json color slugs for 9 tokens (primary-light, accent-light, success, warning, danger, border, highlight, highlight-foreground, gold-muted) — hex known, slug names assumed kebab.
4. `rounded.DEFAULT` = md (none declared).
5. Dark-mode: only 6/16 tokens redefined; toggle mechanism (system vs manual) + full coverage unspecified.
6. Breakpoints — no numeric values in source; responsive contract stated behaviorally.
7. Per-component visual tokens — none in source; appearance lifted from Lovable .tsx at theme build.
8. Loading-state pattern — none in source.
9. Formal accessibility target (WCAG 2.2 AA?) vs readability floor.
10. InkPols + Ledegids layouts; auth-screen copy — not yet mocked/specced.

### `[NOTE]`
- OQ-12: broken Afrikaans copy at `ui-copy-translations.md:346/353` — human re-translation only, never AI-fix.
- UJ protagonists/journeys (OQ-11) pending founder review.

## 2026-06-16 — Finalize

**Reviewer gate:** skipped by choice (spines distill already-validated, locked sources).

**Decisions confirmed:**
- **Dark mode deferred** — light mode only at launch (v1). Dark tokens kept in DESIGN.md as future-ready scaffolding; full coverage + toggle mechanism out of scope for v1. Resolves assumption #5.
- **Accessibility: readability floor only** — NFR-5 floor (legibility, reading-column width, resonance aria-labels, focus states, alt text, palette contrast); no formal WCAG 2.2 AA conformance claim for v1. Real3D flipbook remains an accepted exception. Resolves assumption #9.
- Safe-default assumptions accepted as drafted (token slug names, `rounded.DEFAULT`=md, typography role composition, behavioral breakpoint contract). Left tagged in-file as low-risk implementation notes.

**Mock coverage — no new mocks rendered (deliberate):**
- *Reference-ready surfaces* (tuisblad, lees-storie, lees-gedig, ontdek, skrywerprofiel, skryf, my-profiel, uitdagings-single, gemeenskap): the **Lovable mockup is the authoritative visual reference** — the handoff intentionally keeps tokens + source (not screenshots) as the precise reference. Rendering parallel HTML mocks would duplicate and risk diverging from the locked design. DESIGN.md owns appearance; EXPERIENCE.md owns behavior.
- *Assembly-only / not-yet-mocked surfaces* (InkPols, Ledegids, auth, lidmaatskap, kontak, oor-ink): genuinely not designed upstream. Rendering speculative mocks would invent design — out of bounds. Left as spine-only; flagged in EXPERIENCE.md Open Questions for a future UX pass when those surfaces are designed.

**Doc standards:** editorial-review-structure / -prose available but not run on this fast pass; spines self-edited for clarity. 

**Status:** DESIGN.md and EXPERIENCE.md set to `status: final`, updated 2026-06-16.
**Next:** `bmad-create-architecture` (the required solutioning gate), then `bmad-create-epics-and-stories`.
